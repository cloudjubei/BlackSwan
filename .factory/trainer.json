{
  "name": "blackswan",
  "recordType": "blackswan-run",
  "description": "BlackSwan trains reinforcement-learning agents to trade a SINGLE crypto asset (BTCUSDT) on the spot market, long-only by default, under realistic 0.1% per-trade fees. Each run trains on historical klines and is tested OUT-OF-SAMPLE (walk-forward windows 2022/2023/2024, expanding train); the agent observes multi-timeframe OHLCV (e.g. stacked 1h+1d layers) plus optional technical indicators. The objective is traded_return = total post-fee return gated by trade frequency (trade often AND well — a near-hold run scores ~0). In the strong presets the agent has NO explicit sell action (no_sell_action): it only times entries and exits are forced by take-profit/stop-loss/trailing-stop — so 'many buys, few sells' is by design, not a bug. 'Good' = a setup that trades often and profitably AND beats buy-and-hold on risk-adjusted terms (positive sharpe_alpha) net of fees, stable across seeds and walk-forward windows.",
  "run": ".venv/bin/python -m trainer.run --config-json {configPath} --summary-out {summaryOut}",
  "calibrate": ".venv/bin/python -m trainer.run --calibrate --summary-out {summaryOut}",
  "objective": {
    "name": "traded_return",
    "direction": "max"
  },
  "pipelineVersion": "3",
  "pipelineChangelog": [
    {
      "version": "3",
      "date": "2026-06-16",
      "breaking": false,
      "summary": "Walk-forward evaluation: a walk_forward_window lever tests on 2022/2023/2024 with an expanding train window (default 2024 = train 2020-2023 / test 2024 reproduces the v2 single split EXACTLY, so v2 runs stay comparable to v3 window-2024 runs). Adds sharpe_alpha (risk-adjusted excess over buy-and-hold on the same window) to metrics and stamps the window into the dataset. Adds a sweepable trade_gate_mode (none/linear/quadratic/threshold) for the objective's trade-frequency gate (default quadratic = unchanged). Different windows are separate out-of-sample samples — group them, never compare a 2022 run to a 2024 run as one number."
    },
    {
      "version": "2",
      "date": "2026-06-15",
      "breaking": true,
      "summary": "Data + scoring overhaul: fidelity-scaled rolling windows (QW1) + taker-buy ratio (QW2), 1m-canonical derive-cache + runtime-computed indicators, trade-aware traded_return with a QUADRATIC trade gate, n_trades fixed to completed-trade count, benchmark vs-hold aligned via get_price, combo_noaction default 0 (no synthetic-short bias). Scores are NOT comparable to v1."
    },
    {
      "version": "1",
      "breaking": false,
      "summary": "Original pipeline — the historical results_hour.ods ledger import (Sharpe-era, pre-overhaul). Kept for reference; not comparable to v2."
    }
  ],
  "levers": {
    "model_name": {
      "type": "choice",
      "choices": [
        "reppo-custom",
        "trpo-custom",
        "ppo-custom",
        "duel-dqn-custom",
        "duel-dqn-custom-lstm",
        "munchausen-dqn-custom",
        "qrdqn-custom",
        "dqn",
        "ppo",
        "a2c",
        "hodl"
      ],
      "default": "reppo-custom",
      "description": "The RL algorithm / policy. '-custom' variants use tuned network internals; 'hodl' is the buy-and-hold control."
    },
    "reward_model": {
      "type": "choice",
      "choices": [
        "combo_all2",
        "combo_all",
        "combo_all_fee",
        "combo",
        "combo2",
        "profit_all2",
        "profit_percentage3",
        "profit_percentage_direct",
        "differential_sharpe"
      ],
      "default": "combo_all",
      "description": "Which reward function shapes behaviour. The combo_* family weight profit, drawdown and action-quality differently — each is a distinct approach (intentionally kept separate). combo_all_fee = combo_all plus an explicit per-trade turnover penalty. profit_percentage_direct = the raw per-step portfolio return (direct RL); differential_sharpe = Moody & Saffell online risk-adjusted return (the research-favoured kind for trading) — both reward the equity path itself rather than hand-shaped signals."
    },
    "combo_noaction": {
      "type": "choice",
      "choices": [0, -1],
      "default": 0,
      "description": "Reward multiplier applied to price moves while the agent is FLAT (no position). Default 0 = direction-neutral (a flat agent is neither rewarded nor punished for which way price moves). -1 bakes a synthetic-short incentive (rewards price falling while flat) — kept as a deliberate variant to probe, NOT the default, since the agent is long-only."
    },
    "trade_gate_mode": {
      "type": "choice",
      "choices": [
        "quadratic",
        "linear",
        "threshold",
        "none"
      ],
      "default": "quadratic",
      "description": "How the objective (traded_return) is gated by trade frequency: quadratic = (n/20)^2 (historical default — punishes under-trading steeply); linear = n/20; threshold = full credit at >=20 trades else 0; none = ungated. POST-RUN scoring only (does NOT change the training reward), so it's safe to sweep. Research view: realistic fees already regulate churn, so the gate is a band-aid — 'none' lets vol-targeting/Sharpe self-regulate frequency."
    },
    "learning_rate": {
      "type": "number",
      "default": 0.0001,
      "range": [
        5e-05,
        0.001
      ],
      "description": "Optimizer step size. Too high diverges, too low learns slowly. ~0.0001 is the tuned default."
    },
    "gamma": {
      "type": "number",
      "default": 0.99,
      "range": [
        0.1,
        0.999
      ],
      "description": "Discount factor for future reward (0–1). Higher = more far-sighted."
    },
    "batch_size": {
      "type": "choice",
      "choices": [
        128,
        512,
        4096
      ],
      "default": 512,
      "description": "Samples per gradient update. Larger is steadier but slower."
    },
    "buffer_size": {
      "type": "number",
      "default": 100000,
      "range": [
        1000,
        1000000
      ],
      "description": "Replay-buffer capacity — how many past steps the agent can learn from."
    },
    "learning_starts": {
      "type": "number",
      "default": 20000,
      "range": [
        10,
        50000
      ],
      "description": "Steps of random exploration before learning begins (fills the buffer). 20000 was best in past runs."
    },
    "net_arch": {
      "type": "choice",
      "choices": [
        "8192,512",
        "512,64",
        "512,128,32",
        "512,256,64,32",
        "4096,2048,256,128"
      ],
      "default": "512,64",
      "description": "Hidden-layer sizes of the policy network. Small nets (e.g. 512,64) won in past runs; the big 8192,512 overfit and lost."
    },
    "optimizer_class": {
      "type": "choice",
      "choices": [
        "RMSprop",
        "AdamW",
        "NAdam",
        "Adam"
      ],
      "default": "RMSprop",
      "description": "Gradient optimizer. RMSprop was the tuned choice."
    },
    "activation_fn": {
      "type": "choice",
      "choices": [
        "CELU",
        "LogSigmoid",
        "PReLU",
        "ReLU6",
        "Softsign"
      ],
      "default": "CELU",
      "description": "Neuron activation function. CELU was the tuned choice."
    },
    "episodes": {
      "type": "number",
      "default": 1,
      "range": [
        1,
        10
      ],
      "description": "How many passes over the training data. Too few underfits; the exploration schedule now anneals across episodes."
    },
    "exploration_fraction": {
      "type": "number",
      "default": 0.1,
      "range": [
        0.01,
        0.99
      ],
      "description": "Fraction of training over which the exploration rate (epsilon) anneals from 1.0 to exploration_final_eps. Higher = explore longer before exploiting. DQN-family algorithms."
    },
    "exploration_final_eps": {
      "type": "number",
      "default": 0.2,
      "range": [
        0.01,
        0.5
      ],
      "description": "Final exploration rate (epsilon) the schedule anneals to — the residual randomness kept late in training. DQN-family algorithms."
    },
    "lookback_window": {
      "type": "choice",
      "choices": [
        16,
        32,
        64
      ],
      "default": 32,
      "description": "How many past bars per fidelity layer the agent observes each step (intraday paths only; the single daily path is fixed at 1). Wider = more context but a bigger network input."
    },
    "asset": {
      "type": "choice",
      "choices": [
        "BTCUSDT"
      ],
      "default": "BTCUSDT",
      "scope": "dataset",
      "description": "Which coin to train on. Only BTCUSDT has 1d/1h data today; altcoins need backfill (the data mine). DATASET setting — managed as named datasets, not a model knob."
    },
    "timeframe": {
      "type": "choice",
      "choices": [
        "1d",
        "1h"
      ],
      "default": "1d",
      "description": "Bar size. 1d = fast single-layer path; 1h = the richer multi-layer path. Minute data behaved like noise for the RL models."
    },
    "fidelity_set": {
      "type": "choice",
      "choices": [
        "auto",
        "1d",
        "1h",
        "1h+1d",
        "1h+1d+1w"
      ],
      "default": "auto",
      "scope": "dataset",
      "description": "Multi-fidelity observation stack — which timeframes the agent sees AT ONCE (higher layers are resampled from the base bars, so wider = more temporal context but a bigger network input). auto = follow the timeframe lever (1h -> 1h+1d, else 1d); 1d = fast single daily; 1h = single 1h; 1h+1d = the standard intraday stack; 1h+1d+1w adds the weekly trend. Wider stacks gave more insight empirically. DATASET setting — define named datasets (e.g. '1h' vs '1h+1d') in the Datasets tab and run a model across them. When set (not auto) it overrides timeframe's implicit stack."
    },
    "walk_forward_window": {
      "type": "choice",
      "choices": [
        "2022",
        "2023",
        "2024"
      ],
      "default": "2024",
      "scope": "dataset",
      "description": "Walk-forward evaluation window — which year to TEST on, training on every year before it (2024 = train 2020-2023 / test 2024, the historical split; 2023 = train 2020-2022 / test 2023; 2022 = train 2020-2021 / test 2022). Run all three to see whether a config is robust across different market regimes OUT-OF-SAMPLE rather than tuned to one lucky year. DATASET setting — each window is a separate dataset; read them as a distribution, never as a single number."
    },
    "use_indicators": {
      "type": "boolean",
      "default": false,
      "description": "Add a curated set of indicators (RSI/Williams/Stochastic/Choppiness + tanh-squashed meanReversion/turbulence/OBV + a realized-vol regime and a trend-slope feature) as features, computed at RUNTIME from OHLCV — so it works on BOTH 1d and the 1m-derived 1h path. Past RL runs found indicators didn't help on 1d; re-test under v3 scoring with the added regime/trend context."
    },
    "obs_squash": {
      "type": "choice",
      "choices": [
        "none",
        "clip",
        "tanh"
      ],
      "default": "none",
      "description": "Observation normalization experiment. Several base features exceed the declared [-1,1] observation bound and SB3 does NOT clip, so the network sees raw out-of-range values. none = leave as-is (historical); clip = clip features to [-1,1]; tanh = tanh-squash them. Does respecting the declared bound help or hurt?"
    },
    "transaction_fee": {
      "type": "number",
      "default": 0.001,
      "range": [
        0,
        0.01
      ],
      "scope": "environment",
      "description": "Per-trade fee fraction applied to each trade (models real cost; discourages churn). ENVIRONMENT setting — defines the market, not the model."
    },
    "stop_loss": {
      "type": "number",
      "default": 0.02,
      "range": [
        0,
        0.1
      ],
      "scope": "environment",
      "description": "Auto-close a losing position at this drawdown fraction (0 = off). ENVIRONMENT setting — an exit mechanic, not the model."
    },
    "take_profit": {
      "type": "number",
      "default": 0,
      "range": [
        0,
        0.05
      ],
      "scope": "environment",
      "description": "Auto-close a winning position at this gain fraction (0 = off). ENVIRONMENT setting — an exit mechanic, not the model."
    },
    "trailing_take_profit": {
      "type": "number",
      "default": 0,
      "range": [
        0,
        0.01
      ],
      "scope": "environment",
      "description": "Trailing take-profit fraction that follows the price up (0 = off). ENVIRONMENT setting — an exit mechanic, not the model."
    },
    "no_sell_action": {
      "type": "boolean",
      "default": false,
      "description": "Remove the explicit sell action — exits happen only via TP/SL. Performed well in past runs."
    },
    "position_sizing": {
      "type": "choice",
      "choices": [
        "fixed",
        "vol_target"
      ],
      "default": "fixed",
      "description": "How much of the balance to deploy per entry. fixed = all-in (historical default). vol_target = scale the deployed fraction to target a constant volatility (size = vol_target / recent realized vol, clamped to [0.1, 1]) — risks less in turbulent regimes, the structural fix for 'churns many trades for ~+1%'."
    },
    "vol_target": {
      "type": "number",
      "default": 0.02,
      "range": [
        0.005,
        0.1
      ],
      "description": "Target per-bar volatility for vol_target position sizing (higher = larger positions). Only used when position_sizing = vol_target."
    },
    "allow_shorting": {
      "type": "boolean",
      "default": false,
      "description": "Allow SHORT positions — adds short(3)/cover(4) so the action space becomes 5-way and the agent can profit when price FALLS (essential in down years where long-only can only sit out). Long-only behaviour is unchanged when off; short exits respect TP/SL (mirrored)."
    },
    "device": {
      "type": "choice",
      "choices": [
        "cpu",
        "mps",
        "cuda"
      ],
      "default": "cpu",
      "description": "Compute device: cpu, mps (Apple GPU), or cuda (NVIDIA GPU)."
    },
    "seed": {
      "type": "number",
      "default": 0,
      "description": "Random seed. The same seed reproduces a run exactly; different seeds measure variance."
    }
  },
  "quickStart": {
    "label": "Quick start (fast 1d DQN)",
    "fixed": {
      "model_name": "dqn",
      "net_arch": "512,64",
      "timeframe": "1d",
      "episodes": 1,
      "batch_size": 128,
      "device": "cpu"
    }
  },
  "presets": [
    {
      "label": "▶ Exp 1 · 1d family triage (model × seed, fast)",
      "sweep": { "model_name": ["reppo-custom", "trpo-custom", "duel-dqn-custom", "dqn"] },
      "fixed": {
        "timeframe": "1d",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "episodes": 1,
        "device": "cpu"
      },
      "seeds": 2,
      "thesis": "1d family triage (post-QW1/QW2)",
      "thesisTarget": "model_name"
    },
    {
      "label": "▶ Exp 2 · 1h winners re-baseline (post-features)",
      "sweep": { "model_name": ["reppo-custom", "trpo-custom"] },
      "fixed": {
        "timeframe": "1h",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 4,
      "thesis": "1h winners re-baseline (post-QW1/QW2)",
      "thesisTarget": "model_name"
    },
    {
      "label": "▶ Exp 3 · Trade-frequency frontier (stop_loss)",
      "sweep": { "stop_loss": [0, 0.02, 0.05] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "device": "cpu"
      },
      "seeds": 2,
      "thesis": "trade-frequency frontier",
      "thesisTarget": "stop_loss"
    },
    {
      "label": "▶ Exp 4 · Net capacity re-test (net_arch)",
      "sweep": { "net_arch": ["512,64", "512,256,64,32", "8192,512"] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "device": "cpu"
      },
      "seeds": 2,
      "thesis": "net capacity post-features",
      "thesisTarget": "net_arch"
    },
    {
      "label": "▶ Exp 5 · Indicators on/off — 1h (RB1)",
      "sweep": { "use_indicators": [false, true] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 4,
      "thesis": "indicators on/off (1h, RB1)",
      "thesisTarget": "use_indicators"
    },
    {
      "label": "▶ Exp 6 · Walk-forward robustness (2022·2023·2024)",
      "datasets": [
        { "walk_forward_window": "2022" },
        { "walk_forward_window": "2023" },
        { "walk_forward_window": "2024" }
      ],
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 4,
      "thesis": "is a 1h winner robust across regime-years out-of-sample, or tuned to 2024?",
      "thesisTarget": "walk_forward_window"
    },
    {
      "label": "▶ Exp 7 · Shorting on/off (1h winner)",
      "sweep": { "allow_shorting": [false, true] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 3,
      "thesis": "does enabling short positions beat long-only (esp. in down windows)?",
      "thesisTarget": "allow_shorting"
    },
    {
      "label": "▶ Exp 8 · Vol-targeted sizing (1h winner)",
      "sweep": { "position_sizing": ["fixed", "vol_target"] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 3,
      "thesis": "does vol-targeted sizing beat all-in (fewer trades, steadier Sharpe)?",
      "thesisTarget": "position_sizing"
    },
    {
      "label": "▶ Exp 9 · Direct/recurrent rewards (1h winner)",
      "sweep": { "reward_model": ["combo_all", "profit_percentage_direct", "differential_sharpe"] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 3,
      "thesis": "do equity-path rewards beat the combo shaping family?",
      "thesisTarget": "reward_model"
    },
    {
      "label": "▶ Exp 10 · Fidelity stack (1h winner)",
      "datasets": [
        { "fidelity_set": "1h" },
        { "fidelity_set": "1h+1d" },
        { "fidelity_set": "1h+1d+1w" }
      ],
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 3,
      "thesis": "how much does stacking more timeframes help out-of-sample?",
      "thesisTarget": "fidelity_set"
    },
    {
      "label": "▶ Exp 11 · Obs squash (1h winner)",
      "sweep": { "obs_squash": ["none", "clip", "tanh"] },
      "fixed": {
        "timeframe": "1h",
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "device": "cpu"
      },
      "seeds": 3,
      "thesis": "does respecting the [-1,1] observation bound help?",
      "thesisTarget": "obs_squash"
    },
    {
      "label": "TRPO-custom · 512,64 (~+60%)",
      "fixed": {
        "model_name": "trpo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "timeframe": "1h",
        "device": "cpu"
      }
    },
    {
      "label": "Duel-DQN-custom · 512,256,64,32 (~+60%)",
      "fixed": {
        "model_name": "duel-dqn-custom",
        "net_arch": "512,256,64,32",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "timeframe": "1h",
        "device": "cpu"
      }
    },
    {
      "label": "TRPO-custom · 512,128,32 (~+50%)",
      "fixed": {
        "model_name": "trpo-custom",
        "net_arch": "512,128,32",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "timeframe": "1h",
        "device": "cpu"
      }
    },
    {
      "label": "Reppo-custom · 512,64 (~+50%, win 67%)",
      "fixed": {
        "model_name": "reppo-custom",
        "net_arch": "512,64",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "timeframe": "1h",
        "device": "cpu"
      }
    },
    {
      "label": "Duel-DQN-custom-LSTM · 512,256,64,32 (~+50%)",
      "fixed": {
        "model_name": "duel-dqn-custom-lstm",
        "net_arch": "512,256,64,32",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "timeframe": "1h",
        "device": "cpu"
      }
    },
    {
      "label": "Duel-DQN-custom · 512,128,32 (~+30%, win 80%)",
      "fixed": {
        "model_name": "duel-dqn-custom",
        "net_arch": "512,128,32",
        "reward_model": "combo_all",
        "learning_rate": 0.0001,
        "learning_starts": 20000,
        "batch_size": 512,
        "buffer_size": 1000000,
        "gamma": 0.99,
        "optimizer_class": "RMSprop",
        "activation_fn": "CELU",
        "timeframe": "1h",
        "device": "cpu"
      }
    },
    {
      "label": "Buy & hold (control)",
      "fixed": {
        "model_name": "hodl",
        "timeframe": "1h"
      }
    }
  ],
  "eta": {
    "unitsLever": "episodes"
  },
  "data": [],
  "resources": {
    "gpu": false,
    "memory": "8g",
    "cpus": 4
  }
}
